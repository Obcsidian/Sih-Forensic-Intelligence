# --- Core (required to run the API at all) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 reads bcrypt.__about__.__version__, which bcrypt>=4.1 removed;
# pin to the last 4.x release that still has it.
bcrypt==4.0.1
python-multipart==0.0.9
celery==5.4.0
redis==5.0.8
jinja2==3.1.4
xhtml2pdf==0.2.16

# --- AI layer (lazy-imported; API runs without these, features degrade to
#     a clear "model not installed" response until you install them) ---
faster-whisper==1.0.3
pyttsx3==2.90
sentence-transformers==3.1.1
faiss-cpu==1.8.0.post1
facenet-pytorch==2.6.0
torch==2.2.2
scikit-learn==1.5.2
opencv-python==4.10.0.84
opennsfw2==0.13.0
numpy==1.26.4
Pillow==10.2.0

# --- Forensic image ingestion (lazy-imported; API runs without these,
#     .E01/.S01/.L01/.EX01 case creation degrades to a clear error until
#     you install them) ---
pytsk3==20260715
libewf-python==20240506

# --- Registry hive parsing (lazy-imported; API runs without this, hive files
#     inside an E01/raw image are still extracted as evidence but not parsed
#     into RegistryArtifact rows until you install it) ---
regipy==6.3.0

# --- Dev/test ---
pytest==8.3.3
httpx==0.27.2
